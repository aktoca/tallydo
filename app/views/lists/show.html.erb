<h2><%= @list.name %></h2>

<div class="add"> 
  <% if @list.users.exists?(current_user) %>
  <%= (@list.tasks.mine(current_user).where(done: true).length.to_f / @list.tasks.mine(current_user).length.to_f ) *100 %>%
  <% elsif current_user %>
  <%= button_to( "Add", addUser_path(@list.id)  ) %>
  <% end %>
</div> 


<%= render( partial: "tasks/doable", collection: @list.rankings) %>

