<li>
  <%= link_to list.name, list_path(list.id) %>  

  <% if list.users.exists?(current_user) %>
    <%= (list.tasks.where(done: true).length.to_f / list.tasks.length.to_f ) *100 %>%
  <% elsif current_user %>
    <%= button_to( "Add", addUser_path(list.id)  ) %>
  <% end %>

</li>

